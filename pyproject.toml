[project]
name = "pyabdm-sdk"
version = "0.1.0"
description = "Python SDK for India's Ayushman Bharat Digital Mission (ABDM)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "ruff>=0.14.3",
]
